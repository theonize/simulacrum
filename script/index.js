window.addEventListener('load', event=>{
  const container = document.getElementById('content')
  const links = document.querySelectorAll('a')

  // for (const link of links) {
    a.onclick = event=>{
      container.src = a.href
    }
  // }
})